<% provide(:title, 'All users') %>
<h1>All users</h1>
<% if current_user.admin? %> <%= link_to "New User", new_user_path, data: {no_turbolink: true} %><% end %>
<%= will_paginate %>

<ul class="users">
      <%= render @users %>
</ul>

<%= will_paginate %>
